# Server-side only. Leave empty for explicitly labeled demo results.
API_BASE_URL=https://onz-cocktail.kr/onz
# Optional server-side authorization. Never use a VITE_ prefix for secrets.
API_AUTHORIZATION=
